# ══════════════════════════════════════
# AMPER EN ACCIÓN – Configuración
# Copia este archivo como .env y completa los valores
# ══════════════════════════════════════

# ─── Servidor ───────────────────────────
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500

# ─── Base de datos ───────────────────────
# MongoDB local:
MONGODB_URI=mongodb://localhost:27017/amper_game
# MongoDB Atlas (producción):
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/amper_game

# ─── IA – Generación de preguntas ────────
# Opcional. Si no se especifica, usa el banco de preguntas local.
# OPENAI_API_KEY=sk-...

# ─── Hardware ────────────────────────────
# Opciones: simulation | real
HARDWARE_MODE=simulation

# Si HARDWARE_MODE=real, especifica el tipo:
# Opciones: arduino | raspberry
HARDWARE_TYPE=arduino

# Arduino: puerto serial (Linux: /dev/ttyUSB0, Mac: /dev/tty.usbserial-xxx, Win: COM3)
ARDUINO_PORT=/dev/ttyUSB0

# Raspberry Pi: número de pin GPIO (BCM)
GPIO_PIN=18

# ─── Admin ───────────────────────────────
# Clave para acceder a GET /api/admin/logs
ADMIN_KEY=amper-admin-2024-secret
